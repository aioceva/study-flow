{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "git rev-parse HEAD^ > /dev/null 2>&1 && git diff --quiet HEAD^ HEAD -- . ':(exclude)users/'",
  "buildCommand": "npm run test && npm run build",
  "git": {
    "deploymentEnabled": false
  }
}
