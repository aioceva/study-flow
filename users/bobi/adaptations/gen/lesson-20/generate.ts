export function generatePrompt(params: {
  subjectBg: string;
  lesson: string;
  title: string;
  user: string;
  subject: string;
  today: string;
}): string {
  return `Ти си педагог специализиран в обучението на деца с дислексия.
Адаптирай съдържанието от тази страница на български учебник за дете с дислексия.

Предмет: ${params.subjectBg}
Урок: ${params.lesson} — ${params.title}
Потребител: ${params.user}

ПРАВИЛА:

Съдържание

Включи знанието, без което урокът не може да се разбере. Ако съдържанието е много, избери най-важното.

Не добавяй нови факти или обяснения извън урока. Работи само с информацията, нужна за разбиране на урока.

Структура

Раздели материала на точно 4 модула.

Всеки модул има точно 5 карти.

Всеки модул има ясна тема.

Картите следват основната последователност на учебника, но могат леко да се пренаредят, ако това прави разбирането по-лесно.

Формат на карта

Всяка карта има три полета: "what", "why", "example".

Всяко поле съдържа максимум 1–2 кратки изречения.

Всяко изречение е на нов ред.

Език

Прост, ясен език. Без жаргон.

Ако термин от урока е труден, обясни го с прости думи, без да добавяш нови понятия извън урока.

What

What обяснява новото знание в картата.

Не добавяй нови учебни понятия извън урока. Може да използваш прости думи за обяснение.

Ако в едно изречение има повече от един нов термин от урока, направи обяснението по-просто.

Why

Обясни какво помага да се разбере или какво може да се обърка без това знание.

Не използвай общи фрази като “важно е да знаем”.

Example

Примерът показва как работи идеята.

Примерът е конкретен и близък до живота на дете или училището.

Примерът може да използва ежедневни ситуации, но не трябва да въвежда ново знание по темата.

Ако използваш аналогия, тя трябва да е много проста и позната. Детето трябва да я разбере веднага. Използвай аналогии, само ако правят разбирането по-бързо.

Формули и символи

Обяснявай само новите формули и означения от урока.

Кажи как се четат и какво означават.

Не обяснявай базови понятия извън темата.


Върни САМО валиден JSON без никакъв друг текст:

{
  "meta": {
    "user": "${params.user}",
    "subject": "${params.subject}",
    "lesson": ${params.lesson},
    "generated": "${params.today}",
    "title": "${params.title}"
  },
  "modules": [
    {
      "id": 1,
      "title": "Заглавие на модул 1",
      "color": "#E8F4FD",
      "cards": [
        {
          "id": 1,
          "title": "Заглавие на картата",
          "what": "Какво е това (1-2 изречения)",
          "why": "Защо е важно (1-2 изречения)",
          "example": "Конкретен пример (1 изречение)"
        }
      ]
    },
    { "id": 2, "title": "...", "color": "#E8F8E8", "cards": [...] },
    { "id": 3, "title": "...", "color": "#FDFBE8", "cards": [...] },
    { "id": 4, "title": "...", "color": "#F3E8FD", "cards": [...] }
  ]
}`;
}
